<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/global.css">
    <link rel="stylesheet" href="/css/dashboard.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        .demo-banner {
            background: linear-gradient(135deg, #FF6B6B, #FF8E53);
            color: white;
            padding: 0.5rem 1rem;
            border-radius: 8px;
            margin-top: 0.5rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            font-size: 0.9rem;
            max-width: 400px;
        }
    </style>
</head>
<body>
    <div class="student-layout">
        <aside class="sidebar">
            <div class="sidebar-header">
                <div class="logo">
                    <i class="fas fa-building"></i>
                    <span>PlacementPortal</span>
                </div>
            </div>
            
            <nav class="sidebar-nav">
                <a href="/company/dashboard" class="nav-item active">
                    <i class="fas fa-home"></i>
                    <span>Dashboard</span>
                </a>
                <a href="/company/profile" class="nav-item">
                    <i class="fas fa-building"></i>
                    <span>Company Profile</span>
                </a>
                <a href="/company/post-job" class="nav-item">
                    <i class="fas fa-plus-circle"></i>
                    <span>Post Jobs</span>
                </a>
                <a href="/company/jobs" class="nav-item">
                    <i class="fas fa-briefcase"></i>
                    <span>Manage Jobs</span>
                </a>
                <a href="/company/applicants" class="nav-item">
                    <i class="fas fa-users"></i>
                    <span>Applicants</span>
                </a>
                <a href="/company/analytics" class="nav-item">
                    <i class="fas fa-chart-line"></i>
                    <span>Analytics</span>
                </a>
                <a href="/auth/logout" class="nav-item logout">
                    <i class="fas fa-sign-out-alt"></i>
                    <span>Logout</span>
                </a>
            </nav>
        </aside>

        <main class="main-content">
            <header class="content-header">
                <div class="header-left">
                    <h1>Company Dashboard</h1>
                    <p>Welcome back, <%= user.name %>! 👋</p>
                    <% if (isDemo) { %>
                        <div class="demo-banner">
                            <i class="fas fa-info-circle"></i>
                            <span>You are in demo mode. Create an account to save your data.</span>
                        </div>
                    <% } %>
                </div>
                <div class="header-right">
                    <div class="user-info">
                        <div class="user-avatar">
                            <i class="fas fa-building"></i>
                        </div>
                        <span class="user-name"><%= user.name %></span>
                    </div>
                </div>
            </header>

            <div class="dashboard-content">
                <!-- Stats Cards -->
                <div class="stats-grid">
                    <div class="stat-card">
                        <div class="stat-icon">
                            <i class="fas fa-briefcase"></i>
                        </div>
                        <div class="stat-info">
                            <h3><%= stats.totalJobs || 0 %></h3>
                            <p>Total Jobs</p>
                        </div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-icon">
                            <i class="fas fa-paper-plane"></i>
                        </div>
                        <div class="stat-info">
                            <h3><%= stats.totalApplications || 0 %></h3>
                            <p>Total Applications</p>
                        </div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-icon">
                            <i class="fas fa-user-check"></i>
                        </div>
                        <div class="stat-info">
                            <h3><%= stats.newApplications || 0 %></h3>
                            <p>New Applicants</p>
                        </div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-icon">
                            <i class="fas fa-calendar-check"></i>
                        </div>
                        <div class="stat-info">
                            <h3><%= stats.interviews || 0 %></h3>
                            <p>Interviews</p>
                        </div>
                    </div>
                </div>

                <!-- Quick Actions -->
                <div class="actions-grid">
                    <div class="section-header">
                        <h2>Quick Actions</h2>
                        <p>Manage your hiring process</p>
                    </div>
                    <div class="action-cards">
                        <a href="/company/post-job" class="action-card">
                            <div class="action-icon">
                                <i class="fas fa-plus-circle"></i>
                            </div>
                            <h3>Post New Job</h3>
                            <p>Create new job listings for candidates</p>
                        </a>
                        <a href="/company/applicants" class="action-card">
                            <div class="action-icon">
                                <i class="fas fa-eye"></i>
                            </div>
                            <h3>View Applicants</h3>
                            <p>Review and manage job applications</p>
                        </a>
                        <a href="/company/jobs" class="action-card">
                            <div class="action-icon">
                                <i class="fas fa-tasks"></i>
                            </div>
                            <h3>Manage Jobs</h3>
                            <p>Edit or close existing job postings</p>
                        </a>
                        <a href="/company/analytics" class="action-card">
                            <div class="action-icon">
                                <i class="fas fa-chart-bar"></i>
                            </div>
                            <h3>View Analytics</h3>
                            <p>Track hiring metrics and performance</p>
                        </a>
                    </div>
                </div>

                <!-- Recent Applicants -->
                <div class="applications-section">
                    <div class="section-header">
                        <h2>Recent Applicants</h2>
                        <a href="/company/applicants" class="view-all">View All</a>
                    </div>
                    <div class="applications-list">
                        <% if (recentApplications && recentApplications.length > 0) { %>
                            <% recentApplications.forEach(application => { %>
                                <div class="application-item">
                                    <div class="company-logo">
                                        <i class="fas fa-user-graduate"></i>
                                    </div>
                                    <div class="application-details">
                                        <h4><%= application.student.name %></h4>
                                        <p><%= application.job.title %> • <%= application.student.college || 'College not specified' %></p>
                                        <span class="status status-<%= application.status ? application.status.replace('_', '-') : 'applied' %>">
                                            <%= application.status ? application.status.replace('_', ' ').toUpperCase() : 'APPLIED' %>
                                        </span>
                                    </div>
                                    <div class="application-date">
                                        Applied: <%= application.appliedDate ? application.appliedDate.toLocaleDateString() : new Date().toLocaleDateString() %>
                                    </div>
                                </div>
                            <% }) %>
                        <% } else { %>
                            <div class="no-applications">
                                <p>No applications received yet.</p>
                                <a href="/company/post-job" class="btn-primary">Post Your First Job</a>
                            </div>
                        <% } %>
                    </div>
                </div>

                <!-- Active Jobs Section -->
                <div class="applications-section">
                    <div class="section-header">
                        <h2>Active Job Postings</h2>
                        <a href="/company/jobs" class="view-all">Manage All</a>
                    </div>
                    <div class="applications-list">
                        <% if (activeJobs && activeJobs.length > 0) { %>
                            <% activeJobs.forEach(job => { %>
                                <div class="application-item">
                                    <div class="company-logo">
                                        <i class="fas fa-briefcase"></i>
                                    </div>
                                    <div class="application-details">
                                        <h4><%= job.title %></h4>
                                        <p><%= job.location || 'Location not specified' %> • <%= job.jobType || 'Full-time' %></p>
                                        <span class="status status-active">
                                            ACTIVE
                                        </span>
                                    </div>
                                    <div class="application-date">
                                        Applications: <%= job.applications || 0 %>
                                    </div>
                                </div>
                            <% }) %>
                        <% } else { %>
                            <div class="no-applications">
                                <p>No active job postings.</p>
                                <a href="/company/post-job" class="btn-primary">Create Job Posting</a>
                            </div>
                        <% } %>
                    </div>
                </div>
            </div>
        </main>
    </div>

    <script src="/js/dashboard.js"></script>
</body>
</html>