<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/global.css">
    <link rel="stylesheet" href="/css/dashboard.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        .demo-banner {
            background: linear-gradient(135deg, #FF6B6B, #FF8E53);
            color: white;
            padding: 0.5rem 1rem;
            border-radius: 8px;
            margin-top: 0.5rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            font-size: 0.9rem;
            max-width: 400px;
        }
    </style>
</head>
<body>
    <div class="student-layout">
        <aside class="sidebar">
            <div class="sidebar-header">
                <div class="logo">
                    <i class="fas fa-graduation-cap"></i>
                    <span>PlacementPortal</span>
                </div>
            </div>
            
            <nav class="sidebar-nav">
                <a href="/student/dashboard" class="nav-item active">
                    <i class="fas fa-home"></i>
                    <span>Dashboard</span>
                </a>
                <a href="/student/profile" class="nav-item">
                    <i class="fas fa-user"></i>
                    <span>Profile</span>
                </a>
                <a href="/student/jobs" class="nav-item">
                    <i class="fas fa-briefcase"></i>
                    <span>Job Listings</span>
                </a>
                <a href="/student/applications" class="nav-item">
                    <i class="fas fa-file-alt"></i>
                    <span>My Applications</span>
                </a>
                <a href="/student/resume" class="nav-item">
                    <i class="fas fa-file-pdf"></i>
                    <span>My Resume</span>
                </a>
                <a href="/auth/logout" class="nav-item logout">
                    <i class="fas fa-sign-out-alt"></i>
                    <span>Logout</span>
                </a>
            </nav>
        </aside>

        <main class="main-content">
            <header class="content-header">
                <div class="header-left">
                    <h1>Student Dashboard</h1>
                    <p>Welcome back, <%= user.name %>! 👋</p>
                    <% if (isDemo) { %>
                        <div class="demo-banner">
                            <i class="fas fa-info-circle"></i>
                            <span>You are in demo mode. Create an account to save your data.</span>
                        </div>
                    <% } %>
                </div>
                <div class="header-right">
                    <div class="user-info">
                        <div class="user-avatar">
                            <i class="fas fa-user-graduate"></i>
                        </div>
                        <span class="user-name"><%= user.name %></span>
                    </div>
                </div>
            </header>

            <div class="dashboard-content">
                <!-- Stats Cards -->
                <div class="stats-grid">
                    <div class="stat-card">
                        <div class="stat-icon">
                            <i class="fas fa-briefcase"></i>
                        </div>
                        <div class="stat-info">
                            <h3><%= stats.totalJobs %></h3>
                            <p>Available Jobs</p>
                        </div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-icon">
                            <i class="fas fa-paper-plane"></i>
                        </div>
                        <div class="stat-info">
                            <h3><%= stats.applications %></h3>
                            <p>Applications Sent</p>
                        </div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-icon">
                            <i class="fas fa-check-circle"></i>
                        </div>
                        <div class="stat-info">
                            <h3><%= stats.interviews %></h3>
                            <p>Interviews</p>
                        </div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-icon">
                            <i class="fas fa-clock"></i>
                        </div>
                        <div class="stat-info">
                            <h3><%= stats.pendingApplications %></h3>
                            <p>Pending</p>
                        </div>
                    </div>
                </div>

                <!-- Quick Actions -->
                <div class="actions-grid">
                    <div class="section-header">
                        <h2>Quick Actions</h2>
                        <p>Get things done quickly</p>
                    </div>
                    <div class="action-cards">
                        <a href="/student/jobs" class="action-card">
                            <div class="action-icon">
                                <i class="fas fa-search"></i>
                            </div>
                            <h3>Browse Jobs</h3>
                            <p>Find your dream job from top companies</p>
                        </a>
                        <a href="/student/resume" class="action-card">
                            <div class="action-icon">
                                <i class="fas fa-file-upload"></i>
                            </div>
                            <h3>Upload Resume</h3>
                            <p>Update your resume for better opportunities</p>
                        </a>
                        <a href="/student/profile" class="action-card">
                            <div class="action-icon">
                                <i class="fas fa-edit"></i>
                            </div>
                            <h3>Update Profile</h3>
                            <p>Keep your profile information current</p>
                        </a>
                        <a href="/student/applications" class="action-card">
                            <div class="action-icon">
                                <i class="fas fa-tasks"></i>
                            </div>
                            <h3>Track Applications</h3>
                            <p>Monitor your job application status</p>
                        </a>
                    </div>
                </div>

                <!-- Recent Applications -->
                <div class="applications-section">
                    <div class="section-header">
                        <h2>Recent Applications</h2>
                        <a href="/student/applications" class="view-all">View All</a>
                    </div>
                    <div class="applications-list">
                        <% if (recentApplications && recentApplications.length > 0) { %>
                            <% recentApplications.forEach(application => { %>
                                <div class="application-item">
                                    <div class="company-logo">
                                        <i class="fas fa-building"></i>
                                    </div>
                                    <div class="application-details">
                                        <h4><%= application.job.title %></h4>
                                        <p><%= application.job.company %> • <%= application.job.location %></p>
                                        <span class="status status-<%= application.status ? application.status.replace('_', '-') : 'applied' %>">
                                            <%= application.status ? application.status.replace('_', ' ').toUpperCase() : 'APPLIED' %>
                                        </span>
                                    </div>
                                    <div class="application-date">
                                        Applied: <%= application.appliedDate ? application.appliedDate.toLocaleDateString() : new Date().toLocaleDateString() %>
                                    </div>
                                </div>
                            <% }) %>
                        <% } else { %>
                            <div class="no-applications">
                                <p>You haven't applied to any jobs yet.</p>
                                <a href="/student/jobs" class="btn-primary">Browse Jobs</a>
                            </div>
                        <% } %>
                    </div>
                </div>

                <!-- Profile Completion - Safe Version -->
                <% if (typeof profile !== 'undefined' && profile && profile.profileCompletion !== undefined) { %>
                <div class="profile-completion-section">
                    <div class="section-header">
                        <h2>Profile Completion</h2>
                        <span class="completion-percentage"><%= profile.profileCompletion %>%</span>
                    </div>
                    <div class="completion-bar">
                        <div class="completion-fill" style="width: '<%= profile.profileCompletion || 0 %>%'"></div>
                    </div>
                    <div class="completion-tips">
                        <p>Complete your profile to increase your chances of getting hired:</p>
                        <ul>
                            <% if (!profile.college) { %><li>Add your college/university</li><% } %>
                            <% if (!profile.course) { %><li>Add your course/degree</li><% } %>
                            <% if (!profile.skills || profile.skills.length === 0) { %><li>Add your skills</li><% } %>
                            <% if (!profile.resume) { %><li>Upload your resume</li><% } %>
                        </ul>
                        <a href="/student/profile" class="btn-secondary">Complete Profile</a>
                    </div>
                </div>
                <% } else { %>
                <!-- Default Profile Completion Section -->
                <div class="profile-completion-section">
                    <div class="section-header">
                        <h2>Profile Completion</h2>
                        <span class="completion-percentage">0%</span>
                    </div>
                    <div class="completion-bar">
                        <div class="completion-fill" style="width: 0%;"></div>
                    </div>
                    <div class="completion-tips">
                        <p>Complete your profile to increase your chances of getting hired:</p>
                        <ul>
                            <li>Add your college/university</li>
                            <li>Add your course/degree</li>
                            <li>Add your skills</li>
                            <li>Upload your resume</li>
                        </ul>
                        <a href="/student/profile" class="btn-secondary">Complete Profile</a>
                    </div>
                </div>
                <% } %>
            </div>
        </main>
    </div>

    <script src="/js/dashboard.js"></script>
</body>
</html>